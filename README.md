# orch

OpenTUI React CLI scaffold running on Bun.

## Commands

```bash
bun install
bun run dev
bun run lint
bun run format:check
bun run build
./dist/orch --version
```

The compiled CLI is written to `dist/orch` on macOS/Linux and `dist/orch.exe` on Windows.
