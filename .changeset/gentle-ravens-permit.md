---
"orch": minor
---

Add opencode permission-request handling to the dashboard. Sessions with pending permissions now show in `Needs input`, open a permission dialog before the prompt dialog, and support once, always, deny, or cancel actions with documented shortcuts.
