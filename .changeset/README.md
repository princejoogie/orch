# Changesets

Use `bun run changeset` for user-facing changes that should appear in the release notes.

Use `bun run changeset:version` to apply pending version and changelog updates before cutting a `vX.Y.Z` release.
